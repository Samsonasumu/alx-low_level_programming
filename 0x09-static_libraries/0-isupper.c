#include "main.h"
/**
 * _isupper - this function verifies if a character is uppercase
 * @c: character to be tested
 * Return: return 1 if uppercase 0 if not
 */
int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))
		return (1);
}
