#include <stdio.h>
/**
 *
 * main first prints alphabets in lowercase
 * it always return 0
*/
int main(void)
{
char alph;
for (alph = 'a'; alph <= 'z'; alph++)
putchar(alph);
putchar('\n');
return (0);
}
