#include "main.h"
#include <stdio.h>
/**
 * mul - finds the product
 * @a: the first interger
 * @b: the second integer
 * Return: prodt
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
